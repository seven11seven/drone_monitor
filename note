g++ -std=c++14 gcopter.hpp -I ./ -I /usr/local/include/ompl-1.6/ -o flatness

TODO

 - 1. refine the map_gen package using ROS2; 

2. link the two nodes and visualization;
3. implement the project in docker (two dockers, one for each);
