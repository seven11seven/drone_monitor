1. Eigen is a C++ template library for linear algebra: matrices, vectors, numerical solvers, and related algorithms.

2. inline is used to make fast usage of small and frequently called functions during compling

3. the usage of template function

4. try to reuse the codes from established projects to save time

g++ -std=c++14 -I ./ gcopter.hpp -I /usr/local/include/ompl-1.6/ -o flatness

